"""Schwab provider: Trader API market data + read-only account access.

Mutually exclusive with the ``yahoo`` profile — both bind the same
market-data tool surface and only one can be active at a time.
"""
